#include "main.h"
/**
 * print_numbers - print numbers 0 through 9
 * Return:Always return nothing
 */
void print_numbers(void)
{
	int c;

	for (c >= 0, c <= 9, c++)
	{
		_putchar(c);
	}
	_putchar("\n");
	return (0);
}
