#include <unistd.h>
#include "main.h"
/**
 * _abs - function computes absolute value of integer
 * @int: integer
 * Return: Always 0 (Success)
 */
int _abs(int)
{
	int n;

	if (n < 0)
	m = -1 * n;
	_putchar(m);
	else if (n > 0)
		_putchar(n);
	else
		_putchar(0);
	return (0);
}
